# 인증서 (PC마다 setup.bat으로 생성 — 저장소에 포함 금지)
server/cert.pem
server/key.pem

# 설정 파일 (PC마다 setup.bat으로 생성 — 저장소에 포함 금지)
server/config.json

# Python 가상환경
server/.venv/
__pycache__/
*.pyc

# PyInstaller 빌드 출력
server/build/
server/dist/
